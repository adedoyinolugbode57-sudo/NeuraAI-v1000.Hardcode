# Core web framework
Flask==2.2.5
Flask-Cors==3.0.10

# AI + OpenAI
openai==1.12.0
python-dotenv==1.0.1
requests==2.31.0

# Optional web UI (disable via ENABLE_GRADIO=false)
gradio==3.45.0

# Web server (for Render)
gunicorn==21.2.0

# Optional TTS (online)
gTTS==2.5.1

# Optional audio playback
playsound==1.3.0